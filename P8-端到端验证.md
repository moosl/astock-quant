# P8 — Stage 2 端到端实跑验证

> verifier-2 产出 · 2026-05-16 · Stage 2 P8
>
> 目的：证明 Stage 2 改动未破坏 Stage 1 任何环节；ENABLE_LLM_FACTOR on/off 均可正常运行

---

## 0. 一句话结论

**P8 PASS。Stage 2 完整端到端验证通过：基线与 Stage 1 P5 bit-identical，LLM 因子开启时不阻塞主流程，94/94 单元测试全过，ruff clean。**

---

## 1. 步骤执行结果

### Step 1 — 基线实跑（ENABLE_LLM_FACTOR=0）

| 指标 | 值 | 与 Stage 1 P5 对比 |
|---|---:|---|
| n_features | 25 | 一致 |
| train_size | 25,283 | 一致 |
| valid_size | 5,780 | 一致 |
| accuracy | 0.5334 | bit-identical |
| **AUC** | **0.5131337782587783** | **bit-identical（16 位）** |
| log_loss | 0.6905634407838911 | bit-identical |
| elapsed | 28.6s | — |

**REGRESSION CHECK: PASS** — Stage 2 改动对 Stage 1 无任何影响。

### Step 2 — LLM 启用实跑（ENABLE_LLM_FACTOR=1）

| 指标 | 值 | 备注 |
|---|---:|---|
| n_features | 26 | 含 news_sentiment |
| train_size | 25,283 | 同基线 |
| valid_size | 5,780 | 同基线 |
| accuracy | 0.5334 | 同基线 |
| AUC | 0.5131 | 同基线（bit-identical）|
| log_loss | 0.6906 | 同基线 |
| 新 LLM HTTP 调用 | 9 | 30 票大部分走 cache，少量 cache miss |
| news_sentiment importance | 0.0 | 同 P7 结论 |
| elapsed | 115.5s | 含 LLM cache 查询开销 |
| 非阻塞验证 | PASS | <120s 完成 |

**LLM 因子打开不阻塞主流程：PASS。**

### Step 3 — 全单元测试

```
uv run pytest tests/ -q --tb=short
→ 94 passed in 58.6s
```

**PASS**（94 passed，含 Stage 2 新增 wiring 测试）

### Step 4 — ruff

```
uv run ruff check astock_quant/ tests/
→ All checks passed!
```

**PASS**

---

## 2. Stage 1 回归对比

| 指标 | Stage 1 P5（baseline）| P8 baseline 实跑 | bit-identical |
|---|---:|---:|---:|
| AUC | 0.5131337782587783 | 0.5131337782587783 | **YES** |
| accuracy | 0.5333910034602076 | 0.5333910034602076 | **YES** |
| log_loss | 0.6905634407838911 | 0.6905634407838911 | **YES** |

Stage 2 全部改动（LLM 因子接线 5 处 / max_tokens 256→512 / dataset 新增 source key）对 Stage 1 流水线**零影响**。

---

## 3. Stage 2 整体结论

| 项目 | 结论 |
|---|---|
| Stage 1 回归 | PASS（bit-identical）|
| LLM on/off 均可运行 | PASS |
| LLM 不阻塞主流程 | PASS（<120s）|
| 单元测试 | 94/94 PASS |
| ruff | clean |
| **Stage 2 破坏 Stage 1？** | **否** |

---

## 4. 附注

- moneyflow API 连接失败（东财 30 只全部 `RemoteDisconnected`）是已知问题，Stage 1/2 均如此，不影响主流程（graceful fallback 到空 panel）
- LLM Step 2 出现 9 次新 HTTP 调用（非 0），原因：部分 cache 文件在同一天多次跑时有新 ticker 查询。总花费 < ¥0.01

---

## 附：产出文件

- 本文档：`量化/P8-端到端验证.md`
- `量化/artifacts/p8_baseline_run.json`
- `量化/artifacts/p8_llm_run.json`
